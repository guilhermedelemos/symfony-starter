# Symfony Starter

```
$ git clone git@github.com:guilhermedelemos/symfony-starter.git
$ cd symfony-starter
$ composer install
$ php bin/console about
$ symfony check:security
$ php bin/console debug:router
$ symfony server:start
```